[package]
name = "gitignore"
version = "0.1.1"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rust-embed = { version = "8.11.0", features = ["include-exclude"] }
dirs = "6.0.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
